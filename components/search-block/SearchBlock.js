import styled from "styled-components";import Controls from "./form/Controls";import {FormattedMessage} from "react-intl";const Container = styled.section`  display: flex;  flex-direction: column;  justify-content: center;  align-items: center;  position: relative;  width: 960px;  max-height: 212px;  margin: 0 auto;  background: #FFFFFF;  color: var(--black);`;const MainText = styled.h1`  margin: 36px 0 0 0;  font: 700 24px/32px var(--font-Relaway);`;const SecondaryText = styled.h3`  font: 400 16px/28px var(--font-Noto-sans);  margin-top: 8px;  margin-bottom: 0;`;export default function ({onSearch, products}) {    return (        <Container>            <MainText><FormattedMessage id='title'/></MainText>            <SecondaryText><FormattedMessage id='description'/></SecondaryText>            <Controls onSearch={onSearch}  products={products} />        </Container>    )}